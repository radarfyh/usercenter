#server:
#  port: 8891
spring:
  profiles:
    active: dev
  application:
    name: gateway
  main:
    allow-bean-definition-overriding: true
  cloud:
    alibaba:
      seata:
        tx-service-group: ${spring.application.name}-group
    nacos:
      server-addr: localhost:8848
      discovery:
#        server-addr: localhost:8848
        namespace: a2965a4f-13b2-4b57-8251-fad4411006bd
        group: DEFAULT_GROUP
        server-addr: localhost:8848
      config:
        # nacos 配置中心服务地址 nginx 代理的地址
        # 默认  ${spring.cloud.nacos.server-addr}
#        server-addr: localhost:8848
        file-extension: yml
        prefix: ${spring.application.name}
        # dataID  ${prefix}-${spring.profiles.active}.${file-extension}
        # 命名空间 默认 public
        namespace: a2965a4f-13b2-4b57-8251-fad4411006bd
        # 配置组  默认 DEFAULT_GROUP
        group: DEFAULT_GROUP
        # 共享配置(公共配置)
        server-addr: localhost:8848
#        shared-configs:
#          - data-id: ${spring.application.name}-shared.yml
#            group: DEFAULT_GROUP  # 配置组  默认 DEFAULT_GROUP
#            refresh: true # 是否动态刷新，默认为false


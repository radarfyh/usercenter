spring:
  profiles:
    active: dev
  application:
    name: daas-payment
  main:    allow-bean-definition-overriding: true
  cloud:
    alibaba:
      seata:
        tx-service-group: ${spring.application.name}-group
    nacos:
      server-addr: 127.0.0.1:8848
      discovery:
        namespace: f7c685e5-24eb-4be5-af83-6366b9595bd4
        group: DEFAULT_GROUP
      config:
        file-extension: yml
        prefix: ${spring.application.name}
        namespace: f7c685e5-24eb-4be5-af83-6366b9595bd4
        group: DEFAULT_GROUP


